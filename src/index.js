import React from 'react';
import ReactDOM from 'react-dom';
import AllCharacters from './all_characters';

const { render } = ReactDOM;

/*eslint-env browser*/
render(<AllCharacters />, document.getElementById('all-characters'));
